{
  "name": "throttle-plugin",
  "private": true,
  "version": "1.0.3",
  "description": "Paces Claude Code token usage against the 5-hour rate-limit window.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
